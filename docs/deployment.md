# 📖 Полное пояснение деплоя: Все шаги, варианты и почему выбран каждый

> Гайд для понимания, а не только копи-паста. На каждом шаге — **что это делает**, **какие есть альтернативы**, и **почему выбран именно этот вариант**.

---

# БЛОК 1: Базовая настройка сервера

## Шаг 1.1: Подключение к VM

```bash
ssh root@<YOUR_IP>
```

### Что это делает
Подключаешься по SSH к свежей VM в Yandex Cloud как root (единственный пользователь на новой машине).

### Альтернативы
| Вариант | Как | Когда использовать |
|---------|-----|-------------------|
| **SSH по ключу (сейчас)** | `ssh -i ~/.ssh/id_rsa root@<IP>` | ✅ Стандарт — более безопасно, чем пароль |
| **SSH по паролю** | `ssh root@<IP>` (интерактивно вводишь пароль) | Если ещё нет SSH-ключа (редко) |
| **Консоль Yandex Cloud** | Прямо в веб-интерфейсе — кнопка "Подключиться" | Если SSH не работает, нужна диагностика |

### Почему выбран SSH по ключу
- **Безопаснее:** ключ хранится только на твоём ноутбуке, передаётся не открытым текстом
- **Удобнее:** не нужно вводить пароль каждый раз
- **Стандарт индустрии:** все production-серверы используют ключи
- **Защита от перебора:** пароль подобрать можно (brute-force), ключ из 2048 бит — нельзя

---

## Шаг 1.2: Создание пользователя `deploy`

```bash
adduser deploy && usermod -aG sudo deploy
```

### Что это делает
1. `adduser deploy` → создаёт новый пользователь с домашней папкой `/home/deploy`
2. `usermod -aG sudo deploy` → добавляет его в группу `sudo` (может выполнять команды с `sudo`)

### Альтернативы
| Вариант | Команда | Когда использовать |
|---------|---------|-------------------|
| **useradd вместо adduser** | `useradd -m deploy` | Если adduser недоступен (редко). adduser — дружелюбнее (интерактивный) |
| **Работать только от root** | Просто `sudo` до конца | ❌ Опасно! Одна ошибка может стереть весь сервер |
| **Несколько пользователей** | `adduser app` для приложения, `adduser backup` для бэкапов | Если разграничивать доступ по приложениям (усложнение) |

### Почему выбран этот вариант
- **Безопасность:** если хакер взломает приватный ключ или приложение, у него будут права только `deploy`, а не root
- **Разделение ответственности:** root — только для критических операций, повседневная работа от обычного пользователя
- **Unix-принцип:** каждый пользователь отвечает за свой процесс (Postgres под user `postgres`, Next.js под `deploy` и т.д.)

### Важный момент
После `adduser deploy` интерпретатор спросит пароль и дополнительную информацию. Пароль можно оставить пустым (нажать Enter), потому что логиниться будешь по SSH-ключу, не по паролю.

---

## Шаг 1.3: Копирование SSH-ключей

```bash
rsync --archive --chown=deploy:deploy ~/.ssh /home/deploy
```

### Что это делает
Копирует содержимое папки `~/.ssh` (SSH-ключи root) в `/home/deploy/.ssh` с правильными правами доступа.

**Структура:**
```
~/.ssh/
├── id_rsa              # Приватный ключ (СЕКРЕТНЫЙ!)
├── id_rsa.pub          # Публичный ключ
└── authorized_keys     # Ключи, которым разрешено логиниться (сюда скопируется id_rsa.pub)
```

### Альтернативы
| Вариант | Команда | Минусы |
|---------|---------|--------|
| **rsync (сейчас)** | `rsync --archive --chown=deploy:deploy ~/.ssh /home/deploy` | — |
| **cp с chown** | `cp -r ~/.ssh /home/deploy && sudo chown -R deploy:deploy /home/deploy/.ssh` | Два действия вместо одного |
| **ssh-copy-id** | `ssh-copy-id -i ~/.ssh/id_rsa.pub deploy@localhost` | Работает удалённо, но тут мы локально на сервере |
| **Генерировать новую пару на сервере** | `ssh-keygen -t ed25519` прямо на сервере | Ненужно — хотим использовать свой ключ с ноутбука |

### Почему выбран rsync
- **Одна команда:** копирует + меняет владельца + сохраняет права доступа
- **Безопасно:** `--archive` копирует права (chmod 600 на приватный ключ)
- **Идемпотентно:** если запустить дважды — ничего не сломается

### Критично!
Права на приватный ключ должны быть ровно `600` (только хозяин может читать):
```bash
ls -la /home/deploy/.ssh/
# Должно быть:
# -rw------- id_rsa        (600)
# -rw-r--r-- id_rsa.pub    (644)
```

Если права неправильные, SSH падает с "Permission denied".

---

## Шаг 1.4: Обновление системы

```bash
apt update && apt upgrade -y
```

### Что это делает
1. `apt update` → скачивает свежие списки пакетов из репозиториев
2. `apt upgrade -y` → обновляет установленные пакеты до новых версий

### Альтернативы
| Вариант | Команда | Когда |
|---------|---------|-------|
| **dist-upgrade** | `apt dist-upgrade -y` | Если нужны обновления с удалением конфликтующих пакетов (редко) |
| **Автоматические обновления** | `apt install unattended-upgrades && dpkg-reconfigure unattended-upgrades` | Для production — хорошая идея, но здесь не критично |
| **Пропустить обновления** | Ничего не делать | ❌ Плохо — уязвимости останутся |

### Почему эта стратегия
- **Безопасность:** закрываешь уязвимости в ядре Linux и системных библиотеках
- **Совместимость:** обновления обычно совместимы (особенно `upgrade` vs `dist-upgrade`)
- **Флаг `-y`:** автоматически отвечать "да" на все вопросы (иначе висит и ждёт ввода)

---

## Шаг 1.5: Установка пакетов

```bash
apt install -y ufw fail2ban git curl nginx
```

### Что это делает
Устанавливает 5 пакетов:

| Пакет | Назначение | Альтернативы | Почему этот |
|-------|-----------|-------------|-----------|
| **ufw** | Firewall | iptables (низкоуровневый), firewalld (systemd) | Просто, понятно, Ubuntu-стандарт |
| **fail2ban** | Защита от brute-force SSH | Denyhosts, sshguard | Популярнейший, легко настраивается |
| **git** | Клонирование репо | Скачать ZIP с GitHub | Нужна полная версионная история |
| **curl** | HTTP-запросы (загрузка файлов) | wget, httpie | Везде установлен, самый совместимый |
| **nginx** | Веб-сервер / reverse proxy | Apache, Caddy, HAProxy | Лёгкий, быстрый, много примеров |

### Альтернативы для веб-сервера
```bash
# Вариант 1: Apache (сейчас выбран nginx)
apt install apache2
# Минусы: тяжелее, конфиг сложнее, для простого proxy не нужен

# Вариант 2: Caddy (новый, умный)
# Плюсы: автоматический HTTPS, конфиг проще
# Минусы: менее popular, меньше примеров

# Вариант 3: Не ставить вообще, пустить Next.js напрямую на порт 80
# Минусы: Next.js как корневой процесс рискованно (один краш — всё падает)
```

### Почему именно эти пакеты
- **ufw:** минимум конфигурации, работает, интегрирован в Ubuntu
- **fail2ban:** критична защита от перебора паролей (даже SSH требует его)
- **git:** клонировать код, обновления — обязательно
- **curl:** загружать установщики и скрипты (частая потребность)
- **nginx:** обязателен для production (даже если сейчас работаешь по IP, потом нужен будет для доменов и TLS)

---

## Шаг 1.6: Создание Swap (КРИТИЧНО!)

```bash
fallocate -l 4G /swapfile && chmod 600 /swapfile && mkswap /swapfile && swapon /swapfile
echo '/swapfile none swap sw 0 0' >> /etc/fstab
```

### Что это делает
Создаёшь 4-гигабайтный файл на диске, который ОС будет использовать как виртуальное расширение RAM.

**Процесс:**
1. `fallocate -l 4G /swapfile` → выделяешь 4 ГБ на диске
2. `chmod 600 /swapfile` → защищаешь от чтения другими пользователями
3. `mkswap /swapfile` → инициализируешь как swap
4. `swapon /swapfile` → включаешь **сейчас**
5. `echo '...' >> /etc/fstab` → добавляешь в автозагрузку (при перезагрузке включится автоматически)

### Альтернативы

| Вариант | Команда | Минусы |
|---------|---------|--------|
| **Swap через LVM** | `lvcreate -L 4G -n swap vg0 && mkswap /dev/vg0/swap` | Сложнее, нужен LVM на VM |
| **Больше swap (8 ГБ)** | `fallocate -l 8G` | Диск на производительность не влияет, но может помочь при экстремальной нагрузке |
| **Меньше swap (2 ГБ)** | `fallocate -l 2G` | Может не хватить при одновременной сборке Next.js и Docker |
| **Вообще без swap** | Ничего не делать | ❌ **КРИТИЧЕСКАЯ ОШИБКА** — сервер упадёт OOM при сборке |

### Почему именно 4 ГБ

**Потребление памяти:**
- Docker Supabase (10 контейнеров) → ~3 ГБ
- Next.js сборка (`npm run build`) → ~2 ГБ
- **Итого нужно:** ~5 ГБ
- **Доступно на VM:** 8 ГБ
- **Разница:** 3 ГБ

Если оба процесса стартуют одновременно (что редко), может не хватить. Swap в 4 ГБ — подушка безопасности.

**Почему диск, а не чистая RAM:**
- Диск медленнее в 100+ раз, но это всё равно быстрее, чем падение сервера
- Swap работает только при нехватке памяти (не часто)
- В normal situation используется только RAM

### Почему `fallocate`, а не `dd`
```bash
# Вариант 1: fallocate (сейчас)
fallocate -l 4G /swapfile    # Быстро, просто

# Вариант 2: dd (старый способ)
dd if=/dev/zero of=/swapfile bs=1G count=4    # Медленнее в 10+ раз, вписывает реально
```

`fallocate` создаёт файл за миллисекунды (на современных FS), `dd` — за десятки секунд.

### Проверка
```bash
free -h        # Должно показать Swap: 4.0G
swapon -show   # Должно показать /swapfile
```

---

## Шаг 1.7: Firewall

```bash
ufw allow OpenSSH && ufw allow 80 && ufw allow 443 && ufw --force enable
```

### Что это делает
Открываешь три порта:
- **22 (SSH)** → чтобы ты мог подключиться
- **80 (HTTP)** → чтобы пользователи видели сайт
- **443 (HTTPS)** → чтобы сайт был по HTTPS (потом)

**`--force enable`** включает firewall **сейчас же** (без подтверждения).

### Альтернативы

| Вариант | Команда | Когда |
|---------|---------|-------|
| **Открыть все сразу** | `ufw allow 3000 && ufw allow 8000` (для IP-доступа) | Сейчас — если не хочешь DNS |
| **Белый список IP** | `ufw allow from 192.168.1.100 to any port 3000` | Если доступ только из конкретных мест |
| **Отключить firewall** | `ufw disable` | ❌ Плохо — любой может подобраться к Postgres |
| **iptables напрямую** | `iptables -A INPUT -p tcp --dport 22 -j ACCEPT` | ❌ Сложно, ошибки приводят к потере доступа |

### Почему ufw
- **Простота:** 3 строки вместо 30 для iptables
- **Безопасность:** по умолчанию всё заблокировано (whitelist, не blacklist)
- **Отказоустойчивость:** `--force enable` не блокирует SSH (если бы заблокировал, потерял бы доступ)

### Почему `--force`
Обычно `ufw enable` просит подтверждение (потому что может оборвать SSH). `--force` пропускает подтверждение, потому что мы уже открыли SSH.

---

## Шаг 1.8: Fail2Ban

```bash
systemctl enable --now fail2ban
```

### Что это делает
- `enable` → запустить при загрузке системы
- `--now` → запустить сейчас же

Fail2Ban мониторит `/var/log/auth.log` и блокирует IP после N неудачных попыток SSH.

### Конфиг по умолчанию
```ini
# В /etc/fail2ban/jail.conf (прочитаешь потом)
[sshd]
enabled = true
port = ssh
filter = sshd
maxretry = 5          # 5 неудачных попыток
findtime = 600        # За 10 минут
bantime = 3600        # Блокировка на 1 час
```

### Альтернативы

| Вариант | Когда | Минусы |
|---------|-------|--------|
| **Пропустить fail2ban** | Если очень упрощать | ❌ Уязвивость к brute-force |
| **Port knocking** | Для экстремальной security | Оверкиль для этого проекта |
| **Отключить SSH вообще** | Только консоль Yandex | Неудобно |

### Почему fail2ban важен
- Постоянные попытки взлома из интернета (ботнеты)
- Fail2Ban автоматически блокирует IP за несколько секунд
- Минимальные resource overhead

---

## Шаг 1.9: Конфиг SSH (отключение root и паролей)

```bash
nano /etc/ssh/sshd_config
# Найди и измени:
# PasswordAuthentication no
# PermitRootLogin no

systemctl restart ssh
```

### Что это делает
1. **PasswordAuthentication no** → отключить вход по паролю (только SSH-ключи)
2. **PermitRootLogin no** → запретить логиниться как root

### Альтернативы конфигурации

| Вариант | Конфиг | Когда |
|---------|--------|-------|
| **Ключи + deploy** (сейчас) | `PasswordAuthentication no` + `PermitRootLogin no` | ✅ Production-стандарт |
| **Ключи + root** | `PasswordAuthentication no` | Если ты один и не боишься |
| **Ключи + пароли** | Оставить оба включены | Резервный вариант (усложнение) |
| **Только пароли** | По умолчанию | ❌ Уязвивость к brute-force |

### Почему именно эта конфигурация
- **PasswordAuthentication no:** пароль подобрать можно (brute-force), ключ — нет
- **PermitRootLogin no:** даже если кто-то получит ключ, сможет залогиниться только как `deploy`, не как root
- **Принцип layers:** первый слой (ключи), второй слой (не root)

### Важно перед перезагрузкой SSH
**НИКОГДА не рестартуй SSH без проверки!** Если неправильно отредактировать конфиг, потеряешь доступ.

Проверка перед рестартом:
```bash
sshd -t    # Проверяет синтаксис без рестарта
# Если вывод пустой — OK
# Если ошибка — исправь и попробуй снова
```

---

## ✅ Итого Блок 1

После Блока 1 у тебя:
```
┌─ Безопасность ─────────────────────┐
│ root (отключен) ❌                  │
│ deploy (основной пользователь) ✅  │
│ SSH по ключам ✅                   │
│ Пароли отключены ✅                │
│ Firewall (открыты 22/80/443) ✅    │
│ Fail2Ban включен ✅                │
│ Swap 4 ГБ (подстраховка) ✅        │
└─────────────────────────────────────┘
```

---

# БЛОК 2: Docker Engine

## Шаг 2.1: Установка Docker

```bash
curl -fsSL https://get.docker.com | sh
```

### Что это делает
Загружает и выполняет официальный скрипт Docker:
1. Добавляет репозиторий Docker в `apt`
2. Устанавливает Docker Engine, CLI, containerd
3. Настраивает автозагрузку

### Альтернативы

| Вариант | Команда | Когда |
|---------|---------|-------|
| **Официальный скрипт (сейчас)** | `curl -fsSL https://get.docker.com \| sh` | ✅ Рекомендуемый, всегда актуальный |
| **Ubuntu репозиторий** | `apt install docker.io` | Если не доверяешь скриптам (но версия может быть старой) |
| **Podman (вместо Docker)** | `apt install podman` | Если нужна безопасность на уровне контейнеров (для этого оверкиль) |
| **Установка из исходников** | Компилируем из github.com/moby/moby | ❌ Нет смысла |

### Почему официальный скрипт
- Всегда актуальная версия
- Официально поддерживается Docker Inc.
- Добавляет репозиторий (автоматические обновления)
- Работает на всех Linux-дистрибутивах

### Альтернативный способ (без curl)
```bash
# Если curl не установлен (что-то пошло не так)
wget -qO- https://get.docker.com | sh
```

---

## Шаг 2.2: Добавление deploy в группу docker

```bash
usermod -aG docker deploy
su - deploy
```

### Что это делает
1. Добавляешь `deploy` в группу `docker` (может запускать контейнеры без `sudo`)
2. Переключаешься на пользователя `deploy` (для дальнейшей работы)

### Альтернативы

| Вариант | Команда | Минусы |
|---------|---------|--------|
| **В группу docker** | `usermod -aG docker deploy` | — |
| **Всегда использовать sudo** | `sudo docker ps` везде | Раздражает, каждый раз вводить пароль (но есть NOPASSWD sudoers) |
| **Не давать доступ deploy** | Ничего не делать | ❌ Docker доступен только root (неудобно) |

### Важно!
После `usermod` нужно **переключиться на пользователя** для вступления в силу новой группы:
```bash
su - deploy      # Логиниться как deploy (новая оболочка)
# Или
exec su - deploy # Заменить текущую оболочку
```

Иначе `docker` всё ещё может не работать (группа не обновится в текущей сессии).

### Проверка
```bash
groups     # Должно включать docker
docker ps  # Должно работать без sudo
```

---

## ✅ Итого Блок 2

Теперь на сервере:
- Docker Engine работает
- Пользователь `deploy` может запускать контейнеры
- Автозагрузка настроена

Следующий шаг — настроить конкретный стек Supabase.

---

# БЛОК 3: Self-hosted Supabase

## Шаг 3.1: Подготовка каталога

```bash
sudo mkdir -p /opt && sudo chown deploy:deploy /opt
cd /opt
```

### Что это делает
1. Создаёшь каталог `/opt` (стандартное место для приложений)
2. Делаешь `deploy` его владельцем (может писать туда без `sudo`)
3. Переходишь в этот каталог

### Почему `/opt`, а не другие места?

| Путь | Назначение | Почему не там |
|------|-----------|---------------|
| **`/opt`** (выбран) | Приложения третьих сторон | ✅ Стандарт, чист, отделён от системы |
| `/home/deploy/app` | Домашняя папка пользователя | Технически работает, но захламляет домашнюю папку |
| `/srv/` | Данные сервисов | Более для данных, чем для кода |
| `/usr/local/` | Локальные приложения | Обычно для одиночных бинарников, не для больших приложений |
| `/var/www/` | Веб-сайты (Apache, nginx) | Устаревший подход, для этого проекта не подходит |
| `/app` (в корне) | Корень ФС | ❌ Небезопасно, может конфликтовать с системными каталогами |

### Альтернатива: Docker Volumes
Вместо каталога на диске можно использовать Docker Volumes:
```bash
docker volume create supabase-data
docker compose --mount source=supabase-data,target=/var/lib/postgresql
```
**Минусы:** сложнее бэкапировать, сложнее восстанавливать.

---

## Шаг 3.2: Клонирование репозитория Supabase

```bash
git clone --depth 1 https://github.com/supabase/supabase
mkdir supabase-stack && cd supabase-stack
cp -r ../supabase/docker/* .
cp ../supabase/docker/.env.example .env
```

### Что это делает
1. Клонируешь официальный репо Supabase (флаг `--depth 1` = только последний коммит)
2. Создаёшь папку `supabase-stack` для конкретного экземпляра
3. Копируешь Docker-конфиги и `.env.example`

### Альтернативы клонирования

| Вариант | Команда | Когда |
|---------|---------|-------|
| **Shallow clone (сейчас)** | `git clone --depth 1 ...` | ✅ Быстро, экономит трафик |
| **Полный клон** | `git clone ...` | Если нужна вся история коммитов (редко) |
| **ZIP архив** | Скачать ZIP с GitHub | Если Git недоступен (маловероятно) |
| **Конкретная версия** | `git clone --branch v1.XX.0 --depth 1` | Если нужна старая версия |

### Почему `--depth 1`
История коммитов Supabase — это 10+ тысяч коммитов, много гигабайт. Тебе нужны только текущие файлы, поэтому `--depth 1` экономит трафик и время.

### Структура после копирования
```
/opt/
├── supabase/              # Клон репо (не трогаешь, это шаблон)
│   └── docker/
│       ├── docker-compose.yml
│       ├── .env.example
│       └── ... (10+ файлов)
│
└── supabase-stack/        # Твой экземпляр (заполняешь .env, запускаешь отсюда)
    ├── docker-compose.yml (скопирован)
    ├── .env               (заполняешь секреты)
    └── volumes/           # Данные Postgres, файлы (создаётся автоматически)
```

---

## Шаг 3.3: Генерация секретов

```bash
openssl rand -hex 32   # Запуск 4 раза
```

### Что это делает
Генерирует 64-символьную случайную строку (32 байта = 64 hex-символа).

### Где использовать каждый секрет

| Номер | Переменная | Использование | Опасность если утечёт |
|-------|-----------|----------------|----------------------|
| **1** | `POSTGRES_PASSWORD` | Пароль для БД | Полный доступ к данным |
| **2** | `JWT_SECRET` | Подпись токенов | Можно подделать любой токен |
| **3** | `SECRET_KEY_BASE` | Шифрование GoTrue | Доступ к сессиям пользователей |
| **4** | `VAULT_ENC_KEY` | Шифрование Vault | Доступ к сохранённым паролям API-ключей |

### Альтернативы генерации

| Вариант | Команда | Когда |
|---------|---------|-------|
| **openssl rand (сейчас)** | `openssl rand -hex 32` | ✅ Стандарт, работает везде |
| **Python** | `python3 -c "import secrets; print(secrets.token_hex(32))"` | Если openssl не установлен |
| **dd** | `dd if=/dev/urandom bs=32 count=1 \| xxd -p -c 32` | ❌ Сложно |
| **Manually** | Сидишь и пишешь 64 символа | ❌ **НИКОГДА** — рискованно, не случайно |

### Почему openssl
- Везде установлен
- Криптографически безопасный источник случайности (`/dev/urandom`)
- Одна команда

### Проверка качества
```bash
openssl rand -hex 32 | wc -c
# Должно быть 65 (64 символа + newline)
```

---

## Шаг 3.4: Генерация JWT-ключей (ANON_KEY и SERVICE_ROLE_KEY)

### Что это такое
JWT (JSON Web Token) — это закодированные данные, подписанные `JWT_SECRET`. Две роли:
- **ANON_KEY:** для фронтенда, ограниченные права
- **SERVICE_ROLE_KEY:** для бэкенда, обходит RLS (опасный!)

### Структура JWT
```json
// Header (всегда одинаковый для ALIS256)
{
  "alg": "HS256",
  "typ": "JWT"
}

// Payload (разный для каждой роли)
{
  "role": "anon",           // или "service_role"
  "iss": "supabase",
  "iat": 1704067200,        // issued at (сейчас)
  "exp": 9999999999         // expires (через 10+ лет)
}

// Signature
HMACSHA256(
  base64UrlEncode(header) + "." + base64UrlEncode(payload),
  JWT_SECRET
)
```

### Способ 1: Генератор на сайте (без рисков)

1. Открываешь https://supabase.com/docs/guides/self-hosting#api-keys
2. Вводишь свой `JWT_SECRET`
3. Копируешь два токена (anon и service_role)

**Плюсы:**
- Визуально видишь, что в токене
- Весь процесс в браузере (секрет не отправляется на сервер)

**Минусы:**
- Нужен браузер с интернетом

### Способ 2: Локально через Python

```python
import jwt
import time

secret = "<твой JWT_SECRET>"
now = int(time.time())
exp = now + (365 * 24 * 60 * 60 * 10)  # 10 лет

# ANON_KEY
anon_token = jwt.encode({
    "role": "anon",
    "iss": "supabase",
    "iat": now,
    "exp": exp
}, secret, algorithm="HS256")

# SERVICE_ROLE_KEY
service_token = jwt.encode({
    "role": "service_role",
    "iss": "supabase",
    "iat": now,
    "exp": exp
}, secret, algorithm="HS256")

print(f"ANON_KEY={anon_token}")
print(f"SERVICE_ROLE_KEY={service_token}")
```

**Плюсы:**
- Локально, можно автоматизировать
- Контролируешь даты expiry

**Минусы:**
- Нужен Python и библиотека jwt

### Способ 3: Online JWT Generator

Есть другие сайты типа jwt.io, но их нужно смотреть, потому что некоторые отправляют секрет на сервер.

### Почему два разных ключа

| Где используется | ANON_KEY | SERVICE_ROLE_KEY |
|------------------|----------|-----------------|
| **Фронтенд (браузер)** | ✅ Видна всем | ❌ Никогда! |
| **Server Actions / API** | ❌ Ненужна | ✅ Только здесь |
| **Права доступа** | Ограничены RLS | Обходит RLS |
| **Опасность если утечёт** | Ограничена RLS | КРИТИЧНА — полный доступ |

---

## Шаг 3.5: Заполнение .env

```bash
# Открываешь файл
nano /opt/supabase-stack/.env

# Прописываешь значения
```

### Какие переменные менять

```env
# ===== БЕЗОПАСНОСТЬ (из openssl rand) =====
POSTGRES_PASSWORD=<сгенерированный 1>
JWT_SECRET=<сгенерированный 2>
SECRET_KEY_BASE=<сгенерированный 3>
VAULT_ENC_KEY=<сгенерированный 4>

# ===== API (из JWT-генератора) =====
ANON_KEY=<JWT role=anon>
SERVICE_ROLE_KEY=<JWT role=service_role>

# ===== Дашборд (управление) =====
DASHBOARD_USERNAME=tutor
DASHBOARD_PASSWORD=<сгенерированный новый>

# ===== Адреса (для IP-доступа) =====
SITE_URL=http://<YOUR_IP>:3000
API_EXTERNAL_URL=http://<YOUR_IP>:8000
SUPABASE_PUBLIC_URL=http://<YOUR_IP>:8000
ADDITIONAL_REDIRECT_URLS=http://<YOUR_IP>:3000

# ===== Регистрация и Email =====
DISABLE_SIGNUP=true                  # Только репетитор создаёт учеников
ENABLE_EMAIL_AUTOCONFIRM=true         # Без SMTP (аккаунты создаются с подтверждением)

# ===== Файлы =====
FILE_SIZE_LIMIT=52428800              # 50 МБ для презентаций и материалов
```

### Альтернативные значения

#### SITE_URL / API_EXTERNAL_URL

| Вариант | Значение | Когда |
|---------|----------|-------|
| **По IP (сейчас)** | `http://<YOUR_IP>:3000` и `http://<YOUR_IP>:8000` | Для IP-доступа |
| **По домену** | `https://bio.example.ru` и `https://api.bio.example.ru` | Когда домены готовы |
| **localhost** | `http://localhost:3000` | Только для локальной разработки |

#### DISABLE_SIGNUP

| Значение | Что значит | Почему |
|----------|-----------|--------|
| **true** (сейчас) | Регистрация через UI недоступна | Ученики создаются репетитором через Admin API |
| **false** | Кто угодно может зарегистрироваться | ❌ Уязвивость — злоумышленник создаст аккаунт |

#### ENABLE_EMAIL_AUTOCONFIRM

| Значение | Что значит | Почему |
|----------|-----------|--------|
| **true** (сейчас) | Аккаунты сразу активны | SMTP не нужен, репетитор создаёт аккаунты вручную |
| **false** | Нужно подтверждение письмом | Усложнение, требует SMTP-сервера |

#### FILE_SIZE_LIMIT

| Значение | Для чего | Почему 52428800 |
|----------|----------|-----------------|
| **52428800** (52 МБ, сейчас) | Презентации, видео, большие PDF | Совпадает с `client_max_body_size` в nginx и `bodySizeLimit` в Next.js |
| **10485760** (10 МБ) | Если дешёвый хостинг | Минимум для нормальной работы |
| **104857600** (100 МБ) | Если много видео | Нужно скоординировать с nginx и Next.js |

---

## Шаг 3.6: Правка docker-compose.yml (безопасность)

```bash
nano /opt/supabase-stack/docker-compose.yml
```

Найди секции `db` и `studio`, измени `ports`:

```yaml
# ДО (плохо)
db:
  ports:
    - "5432:5432"

studio:
  ports:
    - "3000:8000"

# ПОСЛЕ (безопасно)
db:
  ports:
    - "127.0.0.1:5432:5432"

studio:
  ports:
    - "127.0.0.1:3000:8000"    # Если вообще Studio нужен, то только локально
```

### Что это делает
Привязывает порты **только** к localhost (127.0.0.1), поэтому снаружи (из интернета) никто не может подключиться.

### Почему это критично

| Конфиг | Снаружи видно | Опасность |
|--------|---------------|-----------|
| **`5432:5432`** | Postgres | Хакер может подключиться и украсть данные |
| **`3000:8000`** | Studio | Хакер может управлять БД, создавать аккаунты |
| **`127.0.0.1:5432:5432`** | Ничего | Безопасно |

### Альтернативы

| Вариант | Синтаксис | Когда |
|---------|-----------|-------|
| **127.0.0.1 (сейчас)** | `127.0.0.1:5432:5432` | ✅ Только локально |
| **0.0.0.0** | `0.0.0.0:5432:5432` | ❌ Видно из интернета |
| **Вообще закрыть** | Не открывать порт | Если управлять БД только через Docker CLI |

---

## Шаг 3.7: Поднять Docker Compose

```bash
cd /opt/supabase-stack
docker compose pull    # Загружаешь образы контейнеров
docker compose up -d   # Запускаешь их
docker compose ps      # Проверяешь статус
```

### Что это делает
1. `pull` → загружает образы всех контейнеров (Postgres, Auth, Storage и т.д.)
2. `up -d` → запускает их в фоне (detached mode)
3. `ps` → показывает статус

### Первый запуск может занять время
```bash
# Когда все контейнеры healthy (может быть долго):
STATUS: running
db: healthy ✓
auth: healthy ✓
storage: healthy ✓
kong: healthy ✓
```

### Альтернативы

| Вариант | Команда | Минусы |
|---------|---------|--------|
| **Фоновый запуск (сейчас)** | `docker compose up -d` | — |
| **Интерактивный запуск** | `docker compose up` (без `-d`) | Видишь логи, но ловишь Ctrl+C — всё падает |
| **С логами** | `docker compose up -d && docker compose logs -f` | Фоновый запуск + просмотр логов |

### Проверка
```bash
docker compose ps
# STATUS должны быть "healthy" или "running"

docker compose logs db
# Должны быть логи инициализации Postgres

netstat -tlnp | grep -E ':(5432|8000)'
# Должны быть слушающие порты на 127.0.0.1
```

---

## ✅ Итого Блок 3

После Блока 3 у тебя:
```
/opt/supabase-stack/
├── docker-compose.yml      ✅ Настроен
├── .env                     ✅ Заполнен секретами
├── volumes/                 ✅ Данные
│   ├── db/                  # Postgres
│   └── storage/             # S3-compatible storage
└── 10 контейнеров запущены ✅
    ├── db (Postgres)        → порт 5432 (только localhost)
    ├── auth (GoTrue)        → часть Kong
    ├── storage              → часть Kong
    ├── kong (API)           → порт 8000 (для клиента)
    ├── studio               → порт 3000 (только localhost)
    └── ... (realtime, vector, backup и т.д.)
```

**Что дальше:** применить схему БД (миграции).

---

# БЛОК 4: Применить схему БД

## Шаг 4.1: Клонирование репозитория приложения

```bash
cd /opt
git clone https://github.com/dimitri-d-morozov/tutor_website.git app
cd app
```

### Что это делает
Клонируешь репозиторий с кодом приложения в `/opt/app`.

### Альтернативы

| Вариант | Когда |
|---------|-------|
| **HTTPS clone (сейчас)** | ✅ Не требует SSH-ключа |
| **SSH clone** | `git clone git@github.com:...` | Если настроен SSH-ключ на сервере |
| **Загрузить ZIP вручную** | Если Git недоступен |

---

## Шаг 4.2: Установка Supabase CLI на сервер

```bash
curl -fsSL https://github.com/supabase/cli/releases/latest/download/supabase_linux_amd64.tar.gz \
  | sudo tar -xz -C /usr/local/bin supabase
```

### Что это делает
Загружает последний бинарник Supabase CLI и кладёт в `/usr/local/bin` (доступен всем как `supabase`).

### Зачем CLI на сервере
Нужно запустить миграции (`.sql` файлы) на продакшене, и `supabase db push` делает это правильно (отслеживает историю, не запускает дважды).

### Альтернативы

| Вариант | Команда | Минусы |
|---------|---------|--------|
| **CLI через curl (сейчас)** | `curl ... \| tar -xz` | — |
| **psql напрямую** | `psql -h localhost -U postgres -d postgres -f migrations/*.sql` | Нет отслеживания истории (может запустить миграцию дважды) |
| **Docker** | `docker run -it supabase/cli db push` | Усложнение |

### Почему именно CLI
- Отслеживает статус миграций в `supabase_migrations.schema_migrations`
- Идемпотентно (не запустит миграцию дважды)
- Безопасно откатывает при ошибке

---

## Шаг 4.3: Узнать порт Postgres

```bash
docker compose -f /opt/supabase-stack/docker-compose.yml port db 5432
# Выведет что-то типа: 127.0.0.1:5432
```

### Что это делает
Узнаёт, на каком порту хоста слушает Postgres (обычно 5432, но можно переконфигурировать).

### Альтернатива (если известно)
Если знаешь, что это 5432 (по умолчанию), можешь пропустить и сразу идти к `db push`.

---

## Шаг 4.4: Запуск миграций

```bash
cd /opt/app
supabase db push --db-url "postgresql://postgres:<POSTGRES_PASSWORD>@127.0.0.1:5432/postgres"
```

### Что это делает
Читает все файлы миграций в `supabase/migrations/` и применяет их к БД.

**Процесс:**
1. Подключается к Postgres
2. Читает таблицу `supabase_migrations.schema_migrations`
3. Вычисляет, какие миграции уже применены
4. Применяет только новые
5. Обновляет таблицу истории

### Альтернативы

| Вариант | Команда | Когда |
|---------|---------|-------|
| **supabase db push (сейчас)** | `supabase db push --db-url ...` | ✅ Правильно, идемпотентно |
| **psql с find** | `find supabase/migrations -name '*.sql' -exec psql ... -f {} \;` | ❌ Неправильно, может запустить дважды |
| **вручную в Studio** | Копировать-пастить каждый файл | ❌ Медленно и ошибкоопасно |

### Важно!
- `<POSTGRES_PASSWORD>` — это значение из `/opt/supabase-stack/.env`
- URL должен быть ровно такой (включая `postgres` в конце)
- Первый запуск может занять время (12 миграций, в т.ч. создание таблиц, индексов, RLS)

### Проверка
```bash
# Если успешно:
# Вывод: "Pushing migrations..." и "✓ Successfully pushed X migrations"

# Если ошибка:
# "error: permission denied for table ..." → нужны GRANT-ы (но должны быть в миграциях)
```

---

## ✅ Итого Блок 4

После Блока 4:
- Все таблицы БД созданы
- RLS-политики включены
- Индексы созданы
- История миграций в `supabase_migrations.schema_migrations`

**Дальше:** создать аккаунт репетитора через Studio.

---

# БЛОК 5: Создать аккаунт репетитора

## Шаг 5.1: SSH-туннель к Studio

```bash
# На ТВОЁМ ноутбуке (не на сервере!)
ssh -L 8000:127.0.0.1:8000 deploy@<YOUR_IP>
```

### Что это делает
Пробивает туннель:
```
Ноутбук:8000 ←→ SSH ←→ Сервер:127.0.0.1:8000 (Studio)
```

**Теперь ты можешь открыть в браузере:**
```
http://127.0.0.1:8000
```

### Почему туннель?
Studio закрыт на сервере (слушает только на 127.0.0.1), его нельзя открыть напрямую. Туннель создаёт локальный порт, связанный с удалённым localhost.

### Альтернативы

| Вариант | Когда | Минусы |
|---------|-------|--------|
| **SSH-туннель (сейчас)** | ✅ Стандарт | Нужно каждый раз пробивать |
| **Открыть Studio в интернет** | Если очень лениво | ❌ Небезопасно — доступен всем |
| **VPN** | Если есть корпоративный VPN | Усложнение |
| **Временно открыть + iptables** | Решение пока не создал аккаунт | Рискованно |

---

## Шаг 5.2: Логин в Studio

```
http://127.0.0.1:8000 → Нажимаешь "Sign In"
```

**Логин:**
- Username: `tutor` (из `DASHBOARD_USERNAME` в `.env`)
- Password: `<DASHBOARD_PASSWORD>` (то, что сгенерировал)

### Альтернатива: прямой запрос к API

Если не хочешь открывать Studio, можешь создать аккаунт через API:
```bash
curl -X POST http://<YOUR_IP>:8000/auth/v1/admin/users \
  -H "Authorization: Bearer <SERVICE_ROLE_KEY>" \
  -H "Content-Type: application/json" \
  -d '{
    "email": "tutor@bio.local",
    "password": "secure_password",
    "email_confirm": true
  }'
```

**Минусы:** нужно в curl вписывать SERVICE_ROLE_KEY (запутанно).

---

## Шаг 5.3: Создать пользователя

В Studio: **Authentication** → **Users** → **Add user**

```
Email: tutor@bio.local
Password: (какой-нибудь сложный пароль)
Auto Confirm User: ✅ (чтобы сразу активным был)
```

Нажимаешь **Create user**.

### Альтернативы

| Вариант | Плюсы | Минусы |
|---------|-------|--------|
| **UI Studio (сейчас)** | Визуально | Нужно Studio |
| **SQL запрос** | Работает везде | Нужно вручную хешировать пароль |
| **API curl** | Автоматизировать можно | Запутанный синтаксис |

---

## Шаг 5.4: Изменить роль на tutor

После создания пользователя триггер `on_auth_user_created` автоматически создаст запись в `profiles` с ролью `student`. Нужно изменить на `tutor`.

**В Studio:** SQL Editor → новая query:

```sql
UPDATE profiles 
SET role = 'tutor' 
WHERE id = '<uuid нового пользователя>';
```

**Где узнать uuid:**
Смотришь в таблице `auth.users` (Studio → Authentication → Users → нажимаешь на пользователя → видишь ID).

### Альтернатива: напрямую в таблице

В Studio → Database → `public` → `profiles` → Edit row → измени `role` на `tutor`.

---

## ✅ Итого Блок 5

Теперь в БД:
- `auth.users` — строка с почтой репетитора
- `profiles` — роль `tutor`

**Дальше:** запустить Next.js, чтобы репетитор мог логиниться.

---

# БЛОК 6: Запустить Next.js

## Шаг 6.1: Установка Node.js

```bash
curl -fsSL https://deb.nodesource.com/setup_24.x | sudo -E bash -
sudo apt install -y nodejs
```

### Что это делает
Добавляет репозиторий NodeSource (актуальные версии Node) и устанавливает Node 24 LTS.

### Альтернативы

| Вариант | Команда | Когда |
|---------|---------|-------|
| **NodeSource (сейчас)** | `curl ... \| sudo -E bash -` | ✅ Актуальные версии, автообновления |
| **Ubuntu репозиторий** | `apt install nodejs` | Версия может быть старой (20 LTS, что тоже ок) |
| **nvm (для разработчика)** | `curl ... \| bash && nvm install 24` | Если разработчик на одной машине с пром-сервером (редко) |
| **Собрать из исходников** | Загрузить с nodejs.org и компилировать | ❌ Оверкиль, долго |

### Проверка
```bash
node --version    # Должно быть >= 20.9
npm --version     # Должно быть актуально
```

---

## Шаг 6.2: Установка зависимостей

```bash
cd /opt/app
npm ci
```

### Что это делает
Устанавливает все npm-пакеты из `package-lock.json` (ровно те версии, которые указаны).

### Почему `npm ci`, а не `npm install`

| Команда | Что делает | Когда |
|---------|-----------|-------|
| **npm ci** (сейчас) | Ставит ровно версии из `package-lock.json` | ✅ Production, нужна стабильность |
| **npm install** | Может обновить минорные версии | Локальная разработка, можно экспериментировать |

**Мораль:** на production всегда `npm ci`.

---

## Шаг 6.3: Создать .env.local

```bash
cd /opt/app

cat > .env.local <<'EOF'
NEXT_PUBLIC_SUPABASE_URL=http://<YOUR_IP>:8000
NEXT_PUBLIC_SUPABASE_ANON_KEY=<ANON_KEY>
SUPABASE_SERVICE_ROLE_KEY=<SERVICE_ROLE_KEY>
EOF

chmod 600 /opt/app/.env.local
```

### Что это делает
Создаёшь файл `.env.local` с переменными окружения.

**Значения берёшь из `/opt/supabase-stack/.env`.**

### Альтернативные адреса

| Вариант | URL | Когда |
|---------|-----|-------|
| **По IP (сейчас)** | `http://<YOUR_IP>:8000` | Для IP-доступа |
| **По домену** | `https://api.bio.example.ru` | Когда домены готовы |
| **localhost** | `http://localhost:8000` | Только для локальной разработки |

### Почему `chmod 600`
`.env.local` содержит `SERVICE_ROLE_KEY` (обходит RLS), поэтому защищаешь от чтения другими пользователями:
```bash
ls -la /opt/app/.env.local
# -rw------- (600) — только владелец может читать
```

### Проверка
```bash
cat /opt/app/.env.local
# Должны видны переменные с IP/доменом и ключи
```

---

## Шаг 6.4: Сборка Next.js

```bash
npm run build
```

### Что это делает
Компилирует TypeScript, оптимизирует бандлы, подготавливает к production.

**Процесс:**
1. Компилирует `.ts` → `.js`
2. Оптимизирует CSS (Tailwind)
3. Создаёт `.next/` папку с готовым приложением
4. Проверяет типы и линтер

### Первая сборка может занять время
```
Creating an optimized production build ...
Compiled successfully

Linting and checking validity of types ...
info  - All checks passed
```

### Если ошибка при сборке

| Ошибка | Причина | Решение |
|--------|---------|---------|
| `Cannot find module 'X'` | Зависимость не установлена | `npm ci` заново |
| `Type error in X` | TypeScript ошибка | Проверь синтаксис |
| `Out of memory` | Недостаточно памяти | Увеличь swap (см. Блок 1) |

### Альтернативы

| Вариант | Команда | Когда |
|---------|---------|-------|
| **Production build (сейчас)** | `npm run build` | ✅ Для deployment |
| **Development mode** | `npm run dev` | Только локально, медленно |

---

## Шаг 6.5: systemd-unit для автозагрузки

```bash
sudo tee /etc/systemd/system/tutor-web.service >/dev/null <<'EOF'
[Unit]
Description=BioPodgotovka Next.js
After=network.target docker.service

[Service]
Type=simple
User=deploy
WorkingDirectory=/opt/app
Environment=NODE_ENV=production
Environment=PORT=3000
ExecStart=/usr/bin/npm run start
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
EOF

sudo systemctl daemon-reload
sudo systemctl enable --now tutor-web
sudo systemctl status tutor-web
```

### Что это делает
Создаёшь systemd-сервис, который:
1. Запускает Next.js при загрузке ОС
2. Перезапускает, если процесс упал
3. Логирует в systemd (видишь через `journalctl`)

### Структура unit-файла

| Секция | Параметр | Значение | Почему |
|--------|----------|----------|--------|
| **[Unit]** | Description | Описание для логов | Визуально ясно |
| | After | `network.target docker.service` | Запускается после сети и Docker (нужны для Supabase) |
| **[Service]** | Type | `simple` | Обычный процесс (не daemon, не notify) |
| | User | `deploy` | Запускается от пользователя (не root) |
| | WorkingDirectory | `/opt/app` | Рабочая папка (откуда читать .env.local) |
| | Environment | `NODE_ENV=production` | Для Node (отключает dev-режим) |
| | Environment | `PORT=3000` | Порт, на котором слушает Next.js |
| | ExecStart | `/usr/bin/npm run start` | Команда для запуска |
| | Restart | `always` | Всегда перезапускать при падении |
| | RestartSec | `5` | Ждать 5 сек перед перезапуском |
| **[Install]** | WantedBy | `multi-user.target` | Включить при загрузке в multi-user mode |

### Альтернативы

| Вариант | Минусы |
|---------|--------|
| **systemd (сейчас)** | — |
| **supervisor** | Отдельный процесс-менеджер (усложнение) |
| **pm2** | Нужен отдельный пакет Node |
| **Запускать вручную** | ❌ Процесс упадёт и не перезапустится |

### Проверка

```bash
sudo systemctl status tutor-web
# Должно быть "active (running)"

sudo journalctl -u tutor-web -f
# Видишь логи в реальном времени
```

---

## ✅ Итого Блок 6

После Блока 6:
- Next.js собран и запущен
- Слушает на порту 3000
- Автоматически перезапускается при падении
- Логирует в systemd

**Следующий шаг:** открыть порты в firewall для IP-доступа.

---

# БЛОК 7: Firewall (для IP-доступа)

## Шаг 7.1: Открыть порты

```bash
ufw allow 3000   # Next.js
ufw allow 8000   # Supabase API
```

### Что это делает
Открываешь ещё два порта в firewall (22/80/443 уже открыты от Блока 1).

### После этого доступно

```
http://<YOUR_IP>:3000   → Next.js (фронтенд)
http://<YOUR_IP>:8000   → Supabase API (для клиента)
```

### Альтернативы

| Вариант | Когда |
|---------|-------|
| **Открыть оба порта (сейчас)** | ✅ Для IP-доступа |
| **Только 80/443 через nginx** | Когда домены будут готовы |
| **Белый список IP** | `ufw allow from 192.168.1.0/24 to any port 3000` — если доступ только из офиса |

---

## ✅ Итого Блок 7

Теперь приложение доступно:
```
Браузер: http://<YOUR_IP>:3000
         ↓ (логин)
         ↓ (создание попыток тестирования)
Supabase API: http://<YOUR_IP>:8000
```

---

# БЛОК 8: Бэкапы (ОБЯЗАТЕЛЬНО!)

## Шаг 8.1: Скрипт бэкапирования

```bash
sudo tee /usr/local/bin/pg-backup.sh >/dev/null <<'EOF'
#!/bin/bash
set -euo pipefail
DIR=/var/backups/supabase && mkdir -p "$DIR"
STAMP=$(date +%F-%H%M)
docker compose -f /opt/supabase-stack/docker-compose.yml exec -T db \
  pg_dumpall -U postgres | gzip > "$DIR/db-$STAMP.sql.gz"
tar czf "$DIR/storage-$STAMP.tar.gz" -C /opt/supabase-stack/volumes storage
find "$DIR" -mtime +14 -delete
EOF

sudo chmod +x /usr/local/bin/pg-backup.sh
```

### Что это делает
1. Сливает всю БД в `.sql.gz`
2. Архивирует папку `storage` (материалы) в `.tar.gz`
3. Удаляет бэкапы старше 14 дней (экономия места)

### Структура бэкапов
```
/var/backups/supabase/
├── db-2026-01-15-0300.sql.gz        # БД каждый день в 3 утра
├── db-2026-01-14-0300.sql.gz
├── storage-2026-01-15-0300.tar.gz   # Файлы материалов
└── storage-2026-01-14-0300.tar.gz
```

### Альтернативы

| Вариант | Минусы |
|---------|--------|
| **pg_dump + tar (сейчас)** | — |
| **Snapshots диска Yandex Cloud** | Привязаны к диску (не помогут если весь диск скомпрометируют) |
| **S3/облако** | Усложнение (нужен бакет S3) |
| **Вообще без бэкапов** | ❌ Личные данные учеников! Сгорит диск → всё потеряется |

---

## Шаг 8.2: Расписание (cron)

```bash
echo "17 3 * * * /usr/local/bin/pg-backup.sh" | sudo crontab -
```

### Что это делает
Запускает скрипт каждый день в 3:17 утра (когда пользователей нет).

**Формат cron:** `минута часть день месяц день_недели команда`
```
17 3 * * * /usr/local/bin/pg-backup.sh
 ↓ ↓ ↓ ↓ ↓
 │ │ │ │ └─ день недели (0=вс, 1=пн...)  → * = каждый день
 │ │ │ └─── месяц (1-12) → * = каждый месяц
 │ │ └───── день месяца (1-31) → * = каждый день
 │ └─────── час (0-23) → 3 = 3 утра
 └───────── минута (0-59) → 17 = 17 минут
```

### Проверка
```bash
sudo crontab -l
# Должна выводить одну строку с бэкапом
```

### Альтернативные расписания

| Расписание | Для чего |
|-----------|----------|
| **3 17 *** (сейчас)** | Один раз в день ночью |
| **0 \* * * *** | Каждый час (много данных) |
| **0 0 * * 0** | Только по воскресеньям (экономия) |

---

## Шаг 8.3: Вывезти бэкапы с сервера

```bash
# На ноутбуке периодически:
rsync -avz deploy@<YOUR_IP>:/var/backups/supabase/ ~/backups/tutor/
```

### Почему нельзя хранить бэкапы на одном диске
Если диск сгорит (hardware fail, хакер удалит), бэкапы тоже пропадут.

**Правило 3-2-1:**
- 3 копии данных
- На 2 разных носителях
- 1 копия вне локации

**Реализация:**
- **Копия 1:** данные на сервере
- **Копия 2:** автоматические бэкапы Yandex (snapshots диска)
- **Копия 3:** ручные бэкапы на ноутбуке (вне сервера)

### Альтернативы

| Вариант | Когда |
|---------|-------|
| **rsync на ноутбук (сейчас)** | ✅ Просто, надёжно |
| **S3 (Yandex Object Storage)** | Если много бэкапов |
| **Telegram-бот** | Если очень мало бэкапов |

---

## Шаг 8.4: Проверка восстановления (КРИТИЧНО!)

```bash
# На тестовой машине (не на production!):
gzip -dc db-2026-01-15-0300.sql.gz | psql -U postgres
```

Бэкап, который никогда не восстанавливали — это не бэкап, это мусор.

---

## ✅ Итого Блок 8

После Блока 8:
- Каждый день в 3:17 утра → новый бэкап БД и файлов
- Бэкапы хранятся 14 дней на сервере
- Периодически вывозишь на ноутбук (вне локации)
- Проверил, что восстановление работает

---

# 🎯 ФИНАЛЬНЫЙ ЧЕКЛИСТ

## Безопасность
- [ ] root отключен
- [ ] Вход только по SSH-ключу (пароли отключены)
- [ ] Firewall включен (22/80/443 открыты, остальное закрыто)
- [ ] fail2ban запущен
- [ ] SERVICE_ROLE_KEY только на сервере в `.env.local` (chmod 600)
- [ ] DISABLE_SIGNUP=true
- [ ] Postgres только на 127.0.0.1
- [ ] Studio только на 127.0.0.1

## Production
- [ ] Swap 4 ГБ включен
- [ ] Docker запущен
- [ ] Supabase здоровый (docker compose ps)
- [ ] Next.js собран (npm run build успешно)
- [ ] systemd-unit tutor-web запущен и включен в автозагрузку
- [ ] Приложение доступно http://<YOUR_IP>:3000

## Доступ
- [ ] Репетитор может логиниться
- [ ] Репетитор может создавать учеников
- [ ] Ученик может видеть занятия и тесты

## Бэкапы
- [ ] Скрипт бэкапирования создан
- [ ] Cron запущен
- [ ] Бэкапы вывозятся на другую машину
- [ ] Восстановление проверено

---

# 📝 ВАРИАНТЫ ВЫБОРА И ПОЧЕМУ

## Архитектура: один VPS vs App Platform + отдельный VPS

**Выбран:** один VPS (всё вместе)

| Критерий | Один VPS | App Platform + VPS |
|----------|----------|-------------------|
| **Стоимость** | ~200-300 руб/мес | ~500+ руб/мес |
| **Сложность** | Проще | Сложнее (две точки обслуживания) |
| **Производительность** | Лучше (нет сетевой задержки) | Хуже (трафик между сервисами) |
| **Масштабируемость** | Хуже | Лучше |
| **Надёжность** | Одна точка отказа | Две точки отказа |

**Вывод:** для одного репетитора и десятков учеников — один VPS дешевле и проще.

---

## Firewall: ufw vs iptables vs firewalld

**Выбран:** ufw

| Критерий | ufw | iptables | firewalld |
|----------|-----|----------|-----------|
| **Сложность** | Простая | Низкоуровневая | Средняя |
| **Нужна ручная конфигурация** | Нет | Да | Да |
| **Риск потери доступа** | Минимален | Высокий | Средний |
| **Популярность на Ubuntu** | ✅ Стандарт | ❌ Редко | ❌ Редко |

**Вывод:** ufw — стандарт на Ubuntu, просто, безопасно.

---

## Сохранение бэкапов: на сервере vs облако vs локально

**Выбран:** на сервере + вывоз на ноутбук (правило 3-2-1)

| Хранилище | Плюсы | Минусы |
|-----------|-------|--------|
| **На сервере** (Вариант 1) | Автоматично | Сгорит если диск упадёт |
| **S3/облако** (Вариант 2) | Безопасно | Платные, ещё один сервис |
| **На ноутбуке** (Вариант 3) | В отдельной локации | Нужно вывозить руками |
| **Все три (3-2-1)** (выбран) | Максимальная защита | — |

**Вывод:** даже простой `rsync на ноутбук` раз в неделю лучше, чем ничего.

---

Этот гайд можно использовать как справочник при каждом шаге. Если что-то не работает — ищешь в таблице "альтернативы" и думаешь, что случилось.

---

# 🔄 ПЕРЕНОС БД НА НОВЫЙ СЕРВЕР

Если нужно мигрировать на новый VPS (например, смена провайдера или upgrade железа).

## 1. На старом сервере: экспорт БД

```bash
# Подключиться к старому серверу
ssh deploy@<OLD_IP>

# Перейти в папку приложения
cd /opt/app

# Сделать свежий дамп БД (с использованием service_role ключа)
docker exec supabase-db pg_dump -U postgres --verbose > db-backup.sql

# Или если нужен сжатый дамп (для больших БД):
docker exec supabase-db pg_dump -U postgres | gzip > db-backup.sql.gz

# Проверить размер
ls -lh db-backup.sql*

# Скачать на ноутбук
exit
scp deploy@<OLD_IP>:/opt/app/db-backup.sql ~/db-backup.sql
```

Отклоняется точка в контейнер (не через localhost:5432) — безопаснее и рабыт из коробки.

## 2. На новом сервере: подготовка

Пройдите **Блоки 1–3** целиком на новом сервере (пользователь, Docker, Supabase).

Новый `.env` для Supabase (новые секреты!):

```bash
cd /opt/supabase-stack
# Если еще нет — скопируйте с старого или сгенерируйте новые ключи
# ВАЖНО: используйте НОВЫЕ ключи, не старые!
nano .env
```

Поднимите Supabase:

```bash
docker compose up -d
docker compose ps  # Проверьте, что все контейнеры healthy
```

## 3. На новом сервере: восстановление БД

```bash
cd /opt/app

# Загрузите файл (scp или копируйте вручную)
# Если сжато:
gunzip -c ~/db-backup.sql.gz | docker exec -i supabase-db psql -U postgres -d postgres

# Если не сжато:
docker exec -i supabase-db psql -U postgres -d postgres < db-backup.sql
```

Ждите — для больших БД может взять минуты.

Проверьте восстановление:

```bash
docker exec supabase-db psql -U postgres -d postgres -c "SELECT count(*) FROM profiles;"
```

Должно вернуть количество учеников.

## 4. На новом сервере: файлы Storage

Если были загруженные материалы (pdf, презентации), нужно перенести и их.

```bash
# На старом сервере: экспортируйте все файлы из контейнера
ssh deploy@<OLD_IP>
docker cp supabase-storage:/var/lib/docker/volumes/supabase-storage/_data storage-files.tar

# Скачайте на ноутбук
exit
scp deploy@<OLD_IP>:/opt/supabase-stack/storage-files.tar ~/storage-files.tar

# На новом сервере: импортируйте
scp ~/storage-files.tar deploy@<NEW_IP>:/opt/supabase-stack/

ssh deploy@<NEW_IP>
cd /opt/supabase-stack
docker cp storage-files.tar supabase-storage:/var/lib/docker/volumes/supabase-storage/_data
```

Это сложнее, чем с БД — зависит от того, как именно организовано хранилище в контейнере.
**Альтернатива:** если материалов немного, репетитор может заново загрузить их в интерфейсе.

## 5. На новом сервере: Code и конфигурация

Скопируйте code и `.env.local`:

```bash
# На новом сервере
cd /opt
git clone <YOUR_REPO> app
cd app

# Скопируйте .env.local со старого сервера (содержит SERVICE_ROLE_KEY)
scp deploy@<OLD_IP>:/opt/app/.env.local .

# Правки в .env.local под новый сервер
nano .env.local
# Обновите: NEXT_PUBLIC_SUPABASE_URL (если изменился IP или домен)
```

## 6. Сборка и запуск на новом сервере

```bash
npm install
npm run build
sudo systemctl start tutor-web
sudo systemctl status tutor-web
```

Проверьте логи:

```bash
sudo journalctl -u tutor-web -n 50
```

Откройте браузер: `http://<NEW_IP>:3000` — должны войти старые ученики, план должен совпадать.

## 7. Переключение DNS

Когда всё работает на новом сервере:

```bash
# В регистраторе домена (reg.ru, Yandex Cloud DNS и т.д.)
# Измените A-запись на новый IP
```

На старом сервере можно помыть и отключить:

```bash
ssh deploy@<OLD_IP>
sudo systemctl stop tutor-web
docker compose down  # Остановить Supabase

# Или полностью удалить VM в облаке
```

## ✅ Чеклист миграции

- [ ] Дамп БД сделан и проверен на старом сервере
- [ ] Новый сервер готов (Блоки 1–3 пройдены)
- [ ] Supabase поднялся и здоровый
- [ ] БД восстановлена, проверена `SELECT count(*) FROM profiles`
- [ ] Storage-файлы перенесены (если нужны)
- [ ] Code и `.env.local` скопированы, обновлены адреса
- [ ] Next.js собран и запущен
- [ ] Логинитесь со старого ученика — всё работает
- [ ] DNS переключен (если используется домен)
- [ ] Старый сервер отключен

**Время:** 30 мин–1 час (в зависимости от размера БД и файлов).

---
