/**
 * Константы материалов, безопасные для импорта в клиентские компоненты.
 *
 * Вынесены из lib/supabase/admin.ts намеренно: тот модуль создаёт service-role
 * клиент, и импорт из клиентского компонента затащил бы его в браузерный бандл.
 */

/** Бакет с файлами материалов (создан миграцией 20260806000006). */
export const MATERIALS_BUCKET = "materials";

/** Лимит размера файла материала — совпадает с лимитом бакета (50 MiB). */
export const MAX_FILE_SIZE = 50 * 1024 * 1024;
